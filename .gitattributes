# Normalize line endings; keep Windows launchers as CRLF so they run when
# double-clicked, and shell scripts as LF.
* text=auto eol=lf
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
*.sh   text eol=lf
